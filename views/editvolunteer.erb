<!DOCTYPE html>
<html>
  <head>
    <title><%= @current_volunteer.name %></title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css'>
  </head>
  <body>
    <div class="container">
      <h1><%= @current_volunteer.name %></h1>
      <hr>
      <p> Would you like to change <%= @current_volunteer.name %>'s name'?</p>
      <div class="form">
        <form action="/volunteers/<%= @current_volunteer.id() %>" method="post">
          <input name="_method" type="hidden" value="patch">
          <label for="name">New name:</label>
          <input id="name" name="name" type="text">
          <p></p>
          <button type="submit">Update Volunteer</button>
        </form>
      </div>
    </div>
  </body>
</html>
